import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles(theme => ({
  spinner: {
    color: "#f0f0f0",
    margin: "0 auto",
    marginTop: "2rem"
  }
}));

export { useStyles };
